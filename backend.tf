# ==============================================================================
# BOOTSTRAP-ПРОБЛЕМА (курка і яйце)
# ==============================================================================
# Цей backend посилається на S3-бакет і DynamoDB-таблицю, які створює модуль
# `s3-backend` (див. main.tf). Поки ці ресурси не існують, Terraform не може
# виконати `init` з увімкненим S3 backend-ом — бакета фізично немає, куди
# писати стейт.
#
# Тому розгортання виконується у ДВА КРОКИ:
#
#   КРОК 1 — bootstrap (локальний стейт):
#     1. Переконайся, що блок `backend "s3" {}` нижче ЗАКОМЕНТОВАНО.
#     2. terraform init
#     3. terraform apply -target=module.s3_backend
#        (створює S3-бакет + DynamoDB-таблицю; стейт лежить локально у файлі
#        terraform.tfstate в цій же директорії)
#
#   КРОК 2 — міграція стейту в S3:
#     1. Розкоментуй блок `backend "s3" {}` нижче та підстав своє реальне ім'я
#        бакета замість "REPLACE-WITH-YOUR-STATE-BUCKET-NAME" (backend-блоки
#        Terraform НЕ підтримують інтерполяцію змінних (var.*), тому значення
#        треба прописати буквально, або передати через `-backend-config` при
#        `terraform init`).
#     2. terraform init -migrate-state
#        (Terraform запитає підтвердження копіювання локального стейту в S3 —
#        погодься, ввівши "yes")
#     3. Переконайся, що `terraform plan` більше не показує змін (стейт успішно
#        мігровано) і видали/заархівуй локальний terraform.tfstate* — надалі
#        джерело істини лежить у S3.
#
# Це ОДИН раз виконаний bootstrap на весь проєкт — наступні уроки (lesson-8-9
# і далі) використовують той самий бакет і той самий key ("terraform.tfstate"),
# бо це один і той самий стейт, що просто розростається новими модулями, а не
# паралельні незалежні стеки.
#
# Покрокові команди з поясненнями — у README.md.
# ==============================================================================

terraform {
  backend "s3" {
    bucket         = "REPLACE-WITH-YOUR-STATE-BUCKET-NAME"
    key            = "terraform.tfstate"
    region         = "us-west-2"
    dynamodb_table = "terraform-state-lock"
    encrypt        = true
  }
}
